/**
 * 帮助文档正文（Markdown 子集，渲染器见 `lib/markdown.ts`）。
 *
 * 为什么放在代码里而不是 JSON：这些是散文，没有需要机器校验的字段；放进数据目录反而会让
 * `data/` 里出现两种性质的文件。生态信息、命令、来源、排错条目仍然在 `data/**` 里被校验。
 *
 * 内容原则：只写我们能兑现的事，不写未实现的计划。口径类内容与 `docs/decisions.md` 保持一致。
 */

export interface HelpDocument {
  id: string;
  title: string;
  summary: string;
  body: string;
}

export const HELP_DOCUMENTS: HelpDocument[] = [
  {
    id: 'usage',
    title: '怎么用',
    summary: '从搜索到改完配置的五步，以及这个站点不会做什么。',
    body: `# 怎么用

1. 在首页搜索框里输入你关心的东西——生态名（\`pip\`、\`npm\`、\`apt\`）、镜像站名或别名（\`清华\`、\`tuna\`）都可以。
2. 进入生态文档后，在「选择来源」里挑一个镜像站。默认选中的是推荐项，点其它来源可以手动改，改过之后自动推荐不会再覆盖你的选择。
3. 「配置命令」给出三种做法：瞬态执行（只影响这一次安装）、持久写入（写进用户级配置）、配置文件（改系统配置文件，附改动对照）。按你的需要选一种复制。
4. 「验证配置」与「恢复与还原」给出确认改对、以及改回去的命令。
5. 遇到问题看「常见问题」，那里按症状列了排查步骤。

## 这个站点会做什么、不会做什么

- 会：打开页面时自动测速——你的浏览器直接请求各镜像站上的一个小文件，每个来源 3 次，用来量它离你多远。
- 会：把测速结果只存在你自己的浏览器里（不上传、没有账号），3 小时内不重复测。
- 不会：修改你的电脑配置。所有命令都由你复制、在你自己的终端里执行。
- 不会：替你判断镜像内容的正确性。同步状态来自镜像站自己发布的文件，没有公开接口的站点显示「同步状态未知」。

## 快捷键与主题

- \`/\` 或 \`Ctrl/Cmd + K\`：唤起搜索框；\`Esc\` 清空。
- 左下角可以切换主题（默认浅色 \`survey\`，深色 \`carbon\`），选择会记住。`,
  },
  {
    id: 'probe',
    title: '测速是怎么算的',
    summary: '响应耗时（估算）的口径、为什么每个来源测三次、同步状态与推荐的关系。',
    body: `# 测速是怎么算的

## 得到的是什么数字

界面上的读数是**响应耗时（估算）**：从你的浏览器发起请求到拿到响应的时间。

它**不等于**下载速度，**不等于** RTT 或 TTFB，也**不能**证明仓库内容正常。它受 DNS 解析、连接复用、
TLS 握手、浏览器调度与对端缓存影响——同一个来源，隔几分钟再测数值会变。

## 什么时候会测速

打开页面就会测一次，不需要你点。为避免反复打扰镜像站，测过的结果会在本地留 **3 小时**：

- 3 小时内再打开任何页面（首页或生态文档），只要网络没变，就直接用本地结果，**一个请求都不发**；
- 超过 3 小时，或者检测到你换了网络（出口 IP 网段变了）就重新测；
- 每个来源各自记时间，所以一次只补测该补的那几个，不会整批重来；
- 测速没成功的来源**不会被覆盖**：屏幕上是它上一次成功的结果，悬停可以看到“最近一次没有成功”，10 分钟内也不会反复去试它；
- 想立刻重测，就到对应生态的文档页点右上角的「测速」（首页不放这个按钮：以后来源多了，在首页一次重测会同时打出很多请求）。

## 为什么每个来源测三次

第一个请求包含建连开销（DNS + TCP + TLS），第二个请求复用同一条连接，所以先测的那次总是偏高。
跨域资源在没有 \`Timing-Allow-Origin\` 时，浏览器会把 \`connectStart\`、\`requestStart\` 这些字段置为 0，
事后**无法把建连时间扣掉**，于是改成多次测量后取**最快两次的平均**，把冷启动那一次排除在外。

- 每个候选占一个并发名额，名额内连续尝试 3 次，任何时刻对外最多 3 个来源在测。
- 连续失败 2 次就停手，不会为一个不可达的来源白等三倍的超时预算。
- 全部超时记「超时」，其它失败记「失败」并保留状态码；\`no-cors\` 的来源只能说明「响应完成，内容未验证」。

## 刻度条与分档词

刻度条画的是固定轴：\`0 – 300 ms\`（0 在最左、300 ms 在最右，每 50 ms 一根细线）。
同一屏里所有来源都用这把尺，因此长短可以直接比较；超过 300 ms 的刻度封顶，右侧仍然给出真实毫秒数。

右侧的分档词（快 / 一般 / 偏慢）另有一套阈值：\`得分 = 1 - 耗时 / 1500ms\`（下限 0），
得分 ≥ 0.75 记「快」，≥ 0.5 记「一般」，其余记「偏慢」。分档与推荐用的是同一个得分，
所以不会出现「刻度短却写着快」这种自相矛盾的显示。

## 同步状态与推荐

同步状态来自镜像站自己发布的机器可读状态文件，只有公开了这类接口的站点才有取值，其余显示
「同步状态未知」。**同步失败或已暂停的来源不会被自动推荐**，但你仍然可以手动选它。

推荐权重为：响应耗时 70、同步状态 20、稳定性 10。官方源（例如 \`pypi.org\`）本身就是上游，
不存在同步这一步，评分按中性处理。`,
  },
];
