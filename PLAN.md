# MirrorN 分阶段实施计划

> 依据：`MAIN.md`。本文件是建议实施顺序，不代表已经实现或验证。
> 目标：小步交付，优先跑通可用闭环，让后续实现模型无需反复做架构决策。

## 一、范围与实施原则

### 首版交付范围

- Vue 3 前端、Hono 轻量后端、共享 Zod 数据契约，使用 pnpm workspace。
- 本地数据驱动的导航、搜索、四步配置向导。
- pip、npm 提供完整向导；apt 首批仅支持明确测试过的 Ubuntu LTS 版本；Docker 先提供官方安装及镜像配置说明，不承诺公共 DockerHub 加速地址长期可用。
- 浏览器轻量探测、推荐排序、缓存和网络变化后的重新探测。
- 后端提供 MirrorZ 状态聚合与可选网络指纹；后端不可用时核心流程仍可使用。
- 基础测试、静态部署与 Docker Compose 部署文档。

### 首版不做

账号、数据库、管理后台、爬虫、自动执行本机命令、任意脚本生成、全生态覆盖、资源上传、网盘转存、LLM、大文件带宽测试。

不引入 Nx/Turborepo、通用工作流引擎或插件系统。优先 Vue composable 与普通 TypeScript 函数；没有明确需求不引入全局状态库。

### 技术选型固定

- 前端：Vue 3、Vite、TypeScript、Vue Router、Tailwind CSS、lucide-vue-next。
- 搜索：Fuse.js；拼音与意图先维护显式别名，不做复杂 NLP。
- 后端：Node.js 当前受支持的 LTS、Hono、Node 适配器。
- 数据：JSON；教程长文本需要时才使用 Markdown，不同时引入 YAML。
- 校验与测试：Zod、Vitest；关键浏览器流程用 Playwright。
- 工具版本在初始化时选兼容组合并锁定，提交 pnpm lockfile。

## 二、开始前必须明确的技术边界

这些约束是对 MAIN.md 中技术描述的修正，实施时应遵守。

1. **轻量探测不等于精确 RTT 或下载速度。** `fetch` 的总耗时受 DNS、TLS、连接复用、缓存及浏览器调度影响。界面统一称“响应耗时（估算）”，不能宣传为精确 TCP RTT、精确 TTFB 或实际下载速度。
2. **no-cors 成功不证明源内容正常。** opaque 响应不可读 HTTP 状态及正文，404 也可能完成请求。探测失败也可能是 CSP、浏览器策略等原因，不能直接宣称站点宕机。同步健康状态来自独立元数据。
3. **网络变化无法被完全捕获。** NetworkInformation 并非所有浏览器支持；出口指纹只反映访问本服务的出口，不能保证识别分流代理到镜像站的路径变化。始终保留手动刷新。
4. **浏览器无法知道本机绝对路径、现有配置和执行结果。** OS 检测只作预选，允许手动修改；显示路径模板和展开说明；验证由用户执行命令并自行核对。
5. **“恢复官方默认”不等于“恢复原配置”。** 前者必须说明配置层级及剩余覆盖项；后者需要事前备份。界面称“复制还原命令”，不能暗示网页已操作本机。
6. **上游 API 与规则需要核实。** MAIN.md 中 MirrorZ 地址、文件名与字段仅作线索，接入前核对真实端点、样例和许可证。chsrc 作为参考，记录来源，不未经审查复制代码或大批规则。
7. **不要默认降低安全性。** SSL 排错优先检查时间、证书链和代理；不默认生成关闭 TLS 验证或 `--trusted-host` 的命令。不自动加 sudo，不覆盖未备份的系统配置。
8. **静态模式从一开始支持。** 指静态 HTTP 托管时不依赖项目后端，不等于断网仍能测速，也不要求双击 file:// 运行或额外引入 SSG。
9. **“0ms 搜索”作为体验愿景而非保证。** 搜索不请求后端、不等待探测；实际性能通过浏览器测量。

## 三、阶段总览

| 阶段 | 目标 | 可交付成果 | 依赖 |
| --- | --- | --- | --- |
| 1 | 骨架与最小数据契约 | 可启动工程、校验过的 pip/npm 数据 | 无 |
| 2 | 静态配置闭环 | 无后端也可完成四步向导 | 1 |
| 3 | 搜索与基础产品体验 | 可检索、可键盘操作的完整前端 | 2 |
| 4 | 浏览器探测与推荐 | 有边界、有缓存的客户端推荐 | 3 |
| 5 | 后端数据增强 | MirrorZ 状态、容灾、网络指纹 | 4 |
| 6 | 首版收口 | 有限 apt/Docker 内容、回归与部署 | 5 |

阶段 3 完成即可发布静态预览版；阶段 6 完成才算首版正式可交付。
每阶段包含多个独立小任务，后续模型一次只实现一个任务，不必一轮完成整阶段。

## 四、各阶段任务与验收

### 阶段 1：工程骨架与数据契约

**状态：已完成**（2026-09-18）。

**1.1 初始化工作区**
- 创建 `apps/web`、`apps/server`、`packages/shared`、`data`。
- 配置 TypeScript strict、格式化、lint 和 Vitest。
- 提供根命令：`pnpm dev`、`pnpm dev:web`、`pnpm build`、`pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm validate:data`。
- `pnpm dev` 启动前后端；`pnpm dev:web` 不启动后端。服务端先仅实现 `/api/health`。

**1.2 定义最小契约与数据校验**
- Zod schema 作为类型来源，使用 `z.infer`，不要另写一套重复接口。
- 最小实体：Mirror、Ecosystem、MirrorSupport、GuideVariant、Troubleshooting。
- Mirror：稳定 ID、名称、主页、可选探测 URL 及探测方式。
- MirrorSupport：生态 ID、镜像 ID、生态专用地址；不可由主页拼接猜测包仓库地址。
- GuideVariant：OS、Shell、可选发行版/版本、临时配置、持久配置、配置文件说明、验证、恢复默认、备份还原说明。
- 不适用的能力显式标记不支持；不要求每个生态都有所有模式。
- 数据记录来源 URL、人工核对日期；检查重复 ID、悬空引用、非 HTTPS 镜像地址、未声明模板变量。

**1.3 添加最小真实数据**
- pip、npm 各录入官方入口及至少两个经核实支持该生态的第三方源。
- 每个生态至少有一份可用向导和一条排错说明。
- 记录规则来源及引用许可；不虚构站点状态或耗时。

**验收**
- 全新安装后可启动、构建；类型检查、基础单测、数据校验通过。
- 前端能读取共享包及静态数据并展示生态列表；后端健康检查成功。
- 故意加入无效数据，校验命令非零退出并指出文件与字段。

**不做：** 完整 UI、上游接口、测速、搜索引擎。

### 阶段 2：静态四步向导

**状态：已完成**（2026-09-18）。排错数据放在 `data/troubleshooting/`；Windows 与 macOS 的命令尚未在真实系统执行，见 `docs/command-validation.md`。

**2.1 实现确定性的命令生成函数**
- 输入：生态、镜像、OS、Shell、配置模式及受限版本选项。
- 输出：前置条件、命令块、配置路径/内容、验证说明、风险和还原说明。
- 只渲染仓库中审核过的模板，变量来自已校验的数据；不接受任意用户命令片段，不使用 eval。
- 生成的命令要在前端离线可用，因此生成器必须零运行时依赖：只用类型和纯函数，不 import zod schema。shared 包若需要同时导出 schema 和生成器，应像 `docs/decisions.md` 那样拆分子路径导出，不要让 zod 重新进入浏览器产物。
- 不支持的组合返回明确原因，不拼出貌似有效的命令。

**2.2 实现向导页面**
- 路由建议 `/`、`/ecosystems/:id`；无效 ID 展示未找到页面。
- 四步：选择系统和终端 → 选择源及配置方式 → 验证 → 恢复/还原。
- pip、npm 支持 Windows PowerShell/CMD、macOS/Linux Bash；不适用内容写清楚。
- 检测 OS 只设置初始值，允许用户覆盖；切换 OS 时修正不兼容 Shell。
- 展示前置安装要求，不将“配置 pip”伪装成“已安装 Python”。
- 支持复制命令、复制失败提示、代码横向滚动及排错折叠卡片。
- 配置文件模式提示合并已有内容；禁止无提示覆盖整个文件。

**2.3 补充生成器和向导测试**
- 覆盖各 OS/Shell 组合、镜像切换、缺失模板、不支持组合、残留占位符。
- 测试实际配置键和 URL，不只生成大量快照。
- 将真实命令人工验证情况登记到 `docs/command-validation.md`；未验证的平台明确标注，不能声称已通过。

**验收**
- 关闭后端，pip/npm 四步流程仍完整可用。
- 选择变化后命令同步变化，无跨终端语法混用。
- 验证与还原说明区分“用户执行”“页面展示”和“恢复原配置”。

**不做：** 测速、自动执行、系统级配置重写。

### 阶段 3：前端搜索与视觉体验

**状态：已完成**（2026-09-18）。镜像新增必需字段 `aliases`（搜索别名）；`换源` 作为意图词命中各生态，未新增单独的“换源总览”页。

**3.1 搜索服务**
- Fuse.js 索引名称、描述、关键词、中文别名、拼音别名。
- 先支持显式映射：`python`/`pip`/`装python`，`node`/`npm`，`换源`、站点名称与拼音。
- 站点命中后展示该站支持的生态入口，不设计无目标的结果卡。
- 添加固定查询测试，包含无结果、空白、大小写及拼音。

**3.2 搜索交互**
- 实现空态、结果态、无结果态及搜索框约 300ms 上移动效。
- 支持 `/`、Ctrl/Cmd+K、上下键、Enter、Esc。
- 普通输入框内不劫持 `/`；处理中文输入法 composition，避免 Enter 误跳转。
- 搜索结果数量先限制为 8 条；后续有实际需求再分页。

**3.3 统一 UI 与无障碍基础**
- 全站使用 Lucide/SVG，不使用 Emoji。
- 固定颜色、字号、间距、圆角和状态样式；完成移动端布局。
- 添加键盘焦点样式、表单标签、状态文字和 reduced-motion 支持。
- 未测速时显示“未测试”，绝不使用演示数字冒充真实状态。

**验收**
- 只用键盘可搜索并进入向导，输入法不误触发导航。
- 搜索完全本地执行；关闭后端不影响结果。
- 窄屏不出现整页横向溢出；动画关闭时功能完整。
- Playwright 覆盖一次“搜索 → 选择生态 → 切换源 → 查看验证说明”。

### 阶段 4：客户端探测、缓存与推荐

**4.1 独立探测服务**
- 新增 ProbeResult schema：镜像 ID、探针 ID、状态、耗时、时间、方式；区分未测、测试中、响应完成、超时、失败。
- 默认每轮最多 6 个候选、并发 3、单请求超时 1500ms，可在配置中调整。
- 同一探针进行中的请求去重；新一轮取消旧任务，旧结果不得覆盖新结果。
- 使用 AbortController；只访问数据中审核过的小资源，不下载大文件。
- no-cors 与 CORS 探针分别处理，opaque 结果标注为“响应完成，内容未验证”。
- 用户确定生态后触发；搜索只对稳定后的前几个候选按需探测，不对每次键入全量扫描。

**4.2 缓存和网络监听**
- localStorage 保存版本号、数据版本、探针标识、结果及过期时间；默认 TTL 15 分钟。
- 防护存储禁用、JSON 损坏、版本不兼容；异常时退回内存。
- 命中缓存立即显示，后台更新同一批候选；30 秒内不重复自动刷新。
- 监听 online/offline、可用时的 connection.change；页面恢复可见时检查是否过期。
- 网络变化后废弃旧结果，防抖并重新探测；离线时停止启动请求。
- 手动刷新绕过 TTL；去重事件监听并在卸载时清理。

**4.3 推荐与 UI 接入**
- 默认采用 MAIN.md 的 70/20/10 权重，但封装成可测试纯函数。
- 延迟分：`clamp(1 - durationMs / timeoutMs, 0, 1)`。
- 同步分：成功 1、同步中 0.5、未知/过期 0.5、失败 0；明确显示未知或过期。
- 稳定性项先给所有来源中性值 0.5，不凭高校或商业身份虚构可靠性。
- 仅本轮成功完成响应且没有明确同步失败的候选参与自动推荐；无候选时保留人工选择，不编造最佳源。
- 排序结果仅是估算参考，平分按稳定 ID 排序；不使用“保证最快”等文案。
- 用户已手动选择镜像后，后台刷新不得自动替换其选择和命令。

**验收**
- 用 mock 覆盖超时、失败、取消、并发上限、缓存过期、损坏缓存和乱序返回。
- 无可用探针时搜索和向导仍正常；界面明确“无法测量”。
- 至少用真实浏览器人工检查一批候选，不把 Node.js 请求结果作为浏览器跨域可用性证据。
- 没有后端仍可测速；结果从不声称精确 RTT 或站点内容健康。

### 阶段 5：后端元数据与网络指纹

**5.1 核实并接入 MirrorZ**
- 先记录真实端点、字段、状态枚举、命名映射、许可与样例到 `docs/upstream.md`，再实现适配器。
- 只接入当前已收录的站点/生态；未知字段和无法匹配的记录安全忽略并记录诊断。
- 动态状态绑定到“镜像 + 生态”，不能用单个站点总状态代替所有仓库状态。
- 本地审核的仓库 URL 和命令规则保持权威，上游状态不可任意改写命令地址。

**5.2 缓存、快照与 API**
- `/api/mirrors` 返回标准化状态、抓取时间、来源、是否过期。
- 每 15 分钟同步；设置超时、响应大小上限和不重叠执行保护。
- 成功校验后才替换内存数据，并原子写入快照。
- 失败保留最近成功数据；启动依次尝试有效快照、内置数据，再后台更新。
- 无数据或过期数据标记未知，不能伪装为实时状态；快照目录可通过 volume 持久化。

**5.3 前端增强与降级**
- 显式配置可选 API 地址；配置后尝试连接，失败快速降级。
- 合并动态状态，不修改本地模板；展示数据更新时间及来源。
- 静态模式不持续轮询不存在的 API；后端故障不影响搜索、向导与本地探测。

**5.4 可选网络指纹**
- `/api/net-fingerprint` 返回使用服务端 secret 做 HMAC 的规范化 IP 网段指纹；不向前端返回原始 IP。
- 默认不信任任意 X-Forwarded-For，仅在配置可信代理时解析转发头。
- 无可靠客户端地址时返回不可用，不能用代理地址伪造有效识别。
- 不声称哈希是完全匿名；不记录原始 IP，接口设置 `Cache-Control: no-store`，无需查询外部 ASN 服务。
- 页面恢复焦点/可见时校验，合并重复事件并限频；指纹变化使缓存失效、取消旧轮次并重测。
- 静态模式或接口不可用时退回阶段 4 策略。

**验收**
- 用固定 fixture 测试归一化；CI 不依赖上游实时可用。
- 覆盖上游超时、非法响应、损坏快照、首次启动无快照、快照写入失败。
- 后端关闭、恢复或状态过期，前端都保持可用。
- 覆盖伪造转发头、指纹不可用及指纹变化后的缓存失效。

### 阶段 6：生态补齐与首版发布

**6.1 有限扩充内容**
- apt 仅支持明确列出的 Ubuntu LTS 版本；按版本区分 sources.list 与 deb822 等配置格式。
- 明确架构、发行版代号、仓库目录、安全更新条目及备份策略；不根据 UA 猜发行版。
- 未支持的发行版/架构阻止生成配置，给出官方文档入口。
- Docker 先区分 Docker CE 软件仓库与 DockerHub 镜像加速；不混为同一种源。
- 公共加速端点未经核实不纳入推荐；首版可只交付说明、官方入口和配置合并注意事项。
- 不整文件覆盖已有 daemon.json，不编造持久可用的加速地址。

**6.2 回归与人工验证**
- 验证 pip/npm 的平台与 Shell 矩阵，登记真实执行证据及未测项。
- apt 在对应一次性 VM/容器中验证适用操作；涉及系统服务的内容使用适当环境，不能由普通容器结果推断全部通过。
- E2E 覆盖搜索、向导、手动选源不被覆盖、API 离线降级、探测失败。
- CI 执行 lint、typecheck、数据校验、单测、构建和关键 E2E。

**6.3 部署与贡献文档**
- README 写明开发命令、静态构建托管、API 配置及功能差异。
- 静态路由采用 hash 模式简化通用托管，或明确记录 history fallback 配置；选定一种并保持一致。
- 提供 Dockerfile 与 docker-compose.yml，支持前后端启动及快照持久化。
- 提供 `.env.example`，不提交 secret；文档说明可信代理和网络指纹隐私边界。
- CONTRIBUTING 给出新增源、生态和排错数据的最小示例与校验命令。
- 汇总第三方许可与署名；补充探测所需 CSP/connect-src 配置及故障排查。

**验收**
- 全新环境按 README 可启动；静态构建在无项目后端时可用。
- `docker compose up --build` 可启动并通过健康检查。
- 所有质量门禁通过，人工验证缺口明确列出；存在阻断性缺口不得宣称正式完成。

## 五、首版之后：独立排期，不提前实现

### 后续 A：按需求扩充生态
- 优先添加 Go、Maven、Cargo 等实际需求最高的生态，每批 2–3 个。
- 每个生态都包含来源、验证、恢复策略、排错和数据校验。
- 大文件带宽测试另立任务：只有确认 CORS、Range 和响应大小约束可行后才加入，并限制流量、时间与取消行为。

### 后续 B：资源共享中心
- 先做静态资源清单、分类、搜索和外部下载链接；社区通过 PR 提交。
- 记录版本、平台、许可、来源和最后核对时间；校验和必须对应具体版本文件，不能给变化链接配固定哈希。
- 不做文件托管、自动网盘转存或用户上传后台。

### 后续 C：LLM 需求识别
- LLM 只输出受 schema 限制的生态 ID、资源 ID 和选项，优先复用已经验证的流程。
- 不直接生成任意系统命令；用户确认后才显示可信模板组装结果。
- 后端管理密钥、限流和超时，AI 故障可退回普通搜索。
- 有真实使用反馈后再决定是否值得投入。

## 六、给后续实现模型的执行协议

每次开始：
1. 阅读 `MAIN.md`、`PLAN.md`、仓库现有规范及当前任务相关文件。
2. 检查现有实现和工作区改动，不重新搭建已经存在的结构，不覆盖用户修改。
3. 只实现指定任务编号，遵守已有契约；必要的契约变更要说明影响，不擅自换技术栈。

每次结束必须汇报：
- 修改了哪些文件，以及实现内容。
- 执行了哪些检查及真实结果；未执行的检查明确写未执行。
- 尚未解决的问题和下一任务建议。
- 不能以占位内容、虚构数据或“理论可行”代替验收。

可直接使用的任务提示词：

> 请阅读 MAIN.md 和 PLAN.md，检查仓库当前状态，实现 PLAN.md 的任务 X.Y。只做本任务及必要依赖，不提前实现后续功能，不引入计划外架构。补充相关测试并执行适用检查。结束时列出变更文件、实际验证结果和未完成项；遇到上游接口、规则或平台行为不明确时先核实，不要猜测。

阶段完成门槛：该阶段任务和验收全部通过；外部阻塞或人工验证缺口必须明确保留，不能提前标记完成。
