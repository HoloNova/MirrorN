# MirrorN · 智能镜像路由与环境就绪平台（文案与架构规范）

> **设计哲学**：极致紧凑 · 强科技感 · 高信噪比 · 零多余修饰。  
> 摒弃传统文档站冗长的说教，采用类似 Linear / Cloudflare / Raycast 的现代开发者工程语言。

---

## 目录
1. [品牌定位与科技感文案体系](#一品牌定位与科技感文案体系)
   - 1.1 品牌 Slogan 与核心关键词
   - 1.2 全站核心文案矩阵（前后对比与精简重塑）
   - 1.3 遥测状态与指标文案（Telemetry Badges）
2. [增补功能模块（增强科技感与实用性）](#二增补功能模块增强科技感与实用性)
   - 2.1 边缘网络遥测卡片（Network Telemetry）
   - 2.2 极速管道流（One-Liner Injection）
   - 2.3 变更差异预览（Config Diff）
3. [Markdown 扩展架构与原子工具规范](#三markdown-扩展架构与原子工具规范)
   - 3.1 极简 Frontmatter 元数据
   - 3.2 现代化短代码/组件语法（MDX 风格）
   - 3.3 目录与热插拔存储拓扑
4. [极客标准 Markdown 模板范例](#四极客标准-markdown-模板范例)
   - 4.1 系统源示例：`ubuntu.md`
   - 4.2 语言生态示例：`pypi.md`

---

## 一、品牌定位与科技感文案体系

### 1.1 品牌 Slogan 与核心关键词

* **品牌标识**：`MirrorN`
* **主标语 (Hero Title)**：  
  **智能镜像路由与开发环境就绪引擎**  
  *(Smart Mirror Router & Developer Ready-State Engine)*
* **副标 (Sub-headline)**：  
  **毫秒级端侧探针 · 动态最优路由 · 零侵入配置生成**
* **核心价值主张（三词流）**：  
  `端侧测速 (Edge Probe)` ｜ `安全隔离 (Zero Mutation)` ｜ `瞬时就绪 (Instant Ready)`

---

### 1.2 全站核心文案矩阵（前后对比与精简重塑）

| 区域 / 模块 | 原有 / 传统文案 (Delete) | 科技感重塑文案 (Adopt) | 调性与逻辑 |
| :--- | :--- | :--- | :--- |
| **首屏 Slogan** | 找到你最快的线路，换源不求人 | **毫秒级测速，一键就绪开发环境** | 剔除小作坊感，强调速度与自动化 |
| **主搜索框** | 搜你想换的软件源... | `检索生态、工具链或系统... [/]` | 极简终端风格，标注快捷键 |
| **测速模块** | 线路测速 / 找到最快的一条 | **延迟遥测矩阵 (Probe Matrix)** | 专业网络术语，更具工业级质感 |
| **推荐源状态** | 最快线路 / 推荐 | `RTT: 18ms · OPTIMAL` | 突出客观指标（Round-Trip Time） |
| **配置流 Step 1** | 选择你的电脑系统和终端 | `01 / 目标环境 (Target Env)` | 数字化序列，精简为极客标签 |
| **配置流 Step 2** | 复制换源命令 | `02 / 路由装配 (Route Config)` | 将“换源”升华为“路由装配” |
| **配置流 Step 3** | 看看配好没 | `03 / 连通校验 (Verify Probe)` | 严谨的工程自检术语 |
| **配置流 Step 4** | 恢复默认源 | `04 / 原生回滚 (Rollback)` | 强调确定性与后悔药机制 |
| **执行模式** | 临时用一下 / 永久生效 | `瞬态执行 (Ephemeral) / 持久写入 (Persistent)` | 计算机科学精准命名 |
| **安全免责** | 这个网站不会碰你的电脑 | `零特权保护：纯内存渲染，无系统写权限` | 明确技术原理，建立极致安全感 |
| **底层安全源** | 安全更新建议用官方源 | `隔离区：安全补丁通道恒定直连官方` | 用“隔离区”替代啰嗦的解释 |

---

### 1.3 遥测状态与指标文案（Telemetry Badges）

统一采用几何指示器与单色微徽标，拒绝一切 Emoji：

* `● 18ms · 极优`（当前网络拓扑下最优 RTT，无丢包）
* `● 45ms · 良好`（延迟平稳，可用作主备节点）
* `● 180ms · 拥塞`（跨网路由或骨干链路拥堵）
* `○ TIMEOUT · 离线`（节点不可达或正在执行上游同步）
* `⚡ SYNCING (99.2%)`（正在增量拉取官方更新，可能有分钟级延迟）

---

## 二、增补功能模块（增强科技感与实用性）

为了让站点跳脱出传统“静态教程站”的框架，建议在文案和布局中补充以下三个现代化模块：

### 2.1 边缘网络遥测卡片 (Network Telemetry)
在搜索框下方或测速区侧边，展示当前客户端的网络物理环境（利用浏览器 API 与轻量出口指纹识别）：
```text
┌─────────────────────────────────────────────────────────────┐
│ 客户端拓扑感知                                               │
│ 出口网络: CERNET (中国教育网)   协议: HTTP/3 (QUIC)         │
│ 本地接入: 华东 / 节点并发探活: 12 Nodes Active               │
└─────────────────────────────────────────────────────────────┘
```
> **文案**：`由本地端侧直连并发探测，计算真实握手耗时，无机房中继偏差。`

### 2.2 极速管道流（One-Liner Injection）
针对高阶开发者，提供一行命令完成安全注入：
```text
┌─────────────────────────────────────────────────────────────┐
│ 极速装配 (CLI 流)                                            │
│ curl -sSL https://mirror.campuslink.vip/pypi | sh -s -- -y │
└─────────────────────────────────────────────────────────────┘
```
> **文案**：`单行命令流，内置自动备份与环境嗅探。`

### 2.3 变更差异预览 (Config Diff)
直接展示配置文件改动前后的 Diff 对比，新手一目了然：
```diff
# /etc/apt/sources.list
- deb http://archive.ubuntu.com/ubuntu/ noble main restricted
+ deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ noble main restricted
```
> **文案**：`原子替换目标 URL，保留系统原装注释与个性化配置。`

---

## 三、Markdown 扩展架构与原子工具规范

### 3.1 极简 Frontmatter 元数据

移除冗余字段，仅保留路由、上游以及系统版本映射：

```yaml
---
id: pypi
title: Python (PyPI)
category: lang           # os | lang | container | tool
upstream: pypi           # 对应的全局镜像站上游标识
platforms: [windows, macos, linux]
default_mode: persistent # persistent(全局) | ephemeral(临时)
last_mod: 2026-09-21
---
```

---

### 3.2 现代化短代码/组件语法（MDX 风格）

采用类 MDX 自闭合或块级标签，直观且便于解析器转换为 Vue/React 组件。

#### 1. 延迟测速矩阵 `<Probe />`
```markdown
<Probe upstream="pypi" />
```
* **表现**：渲染带有动态毫秒计时的镜像节点卡片，根据延迟实时从低到高冒泡排序。

#### 2. 命令装配器 `<ConfigBox />`
```markdown
<ConfigBox upstream="pypi" allow-modes="persistent,ephemeral" />
```
* **表现**：根据用户选中的 OS 与最优镜像，动态输出命令代码块，右上角提供 `COPY` 键与复制动效。

#### 3. 差异对比卡片 `<Diff />`
```markdown
<Diff file="~/.pip/pip.conf">
- index-url = https://pypi.org/simple
+ index-url = {{MIRROR_URL}}/simple
</Diff>
```

#### 4. 高速直链盒 `<Download />`
```markdown
<Download 
  title="Ubuntu 24.04 LTS Desktop"
  path="/releases/24.04/ubuntu-24.04-desktop-amd64.iso"
  size="5.8 GB"
  sha256="8251e68fe75b5b0d0c34ff8031d257ec813b194fb84a0d93a74659b81b29a25b"
/>
```

#### 5. 还原防线 `<Rollback />`
```markdown
<Rollback upstream="pypi" />
```

---

### 3.3 目录与热插拔存储拓扑

服务器端按统一平铺架构组织文档，上传即生效，接口只读消费：

```text
/server/content/
  ├── registry.json             # 全局目录索引缓存 (自动由后台生成)
  ├── os/
  │   ├── ubuntu.md
  │   ├── debian.md
  │   └── arch.md
  ├── lang/
  │   ├── pypi.md
  │   ├── npm.md
  │   ├── cargo.md
  │   └── go.md
  └── container/
      ├── docker.md
      └── conda.md
```

* **管理端接口**：`POST /api/docs/sync` —— 批量/单文件更新 Markdown。
* **客户端接口**：`GET /api/docs/:category/:id` —— 直接返回解析后的结构化数据。

---

## 四、极客标准 Markdown 模板范例

### 4.1 系统源示例：`ubuntu.md`

```markdown
---
id: ubuntu
title: Ubuntu APT 软件源
category: os
upstream: ubuntu
platforms: [linux]
default_mode: persistent
last_mod: 2026-09-21
---

## 01 / 边缘延迟遥测

<Probe upstream="ubuntu" />

---

## 02 / 路由装配

> **隔离策略**：安全补丁（Security Updates）默认走官方源，防止镜像同步滞后引发漏洞风险。

<ConfigBox upstream="ubuntu" default-target="24.04" />

---

## 03 / 连通校验

执行刷新并观察索引响应：

```bash
sudo apt update
```

* `命中 (Hit)` 或 `获取 (Get)` 域名前缀匹配当前推荐节点即为成功。

---

## 04 / 原生回滚

<Rollback upstream="ubuntu" />
```

---

### 4.2 语言生态示例：`pypi.md`

```markdown
---
id: pypi
title: Python (PyPI)
category: lang
upstream: pypi
platforms: [windows, macos, linux]
default_mode: persistent
last_mod: 2026-09-21
---

## 01 / 延迟遥测

<Probe upstream="pypi" />

---

## 02 / 路由装配

<ConfigBox upstream="pypi" />

---

## 03 / 异常排查 (Fault Isolation)

### 证书链路异常 (`SSLError`)
抓包软件或企业内网网关拦截可能破坏 TLS 证书链。在受信任环境下附加信任标识：

```bash
pip install <pkg> --trusted-host {{MIRROR_HOST}}
```

### 终端指令未找到 (`Command Not Found`)
Python Scripts 路径未注入系统 PATH。请将 `%APPDATA%\Python\Scripts` (Windows) 添加至环境变量。

---

## 04 / 原生回滚

重置为官方源：

```bash
pip config unset global.index-url
```
```
